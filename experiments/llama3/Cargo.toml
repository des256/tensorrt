[package]
name = "llama3"
version = "0.1.0"
edition = "2024"

[features]
trtllm = ["tensorrt/trtllm"]

[dependencies]
tensorrt = { path = "../../tensorrt" }
onnx = { path = "../../onnx" }
tokenizers = { version = "0.21", default-features = false, features = ["onig"] }
