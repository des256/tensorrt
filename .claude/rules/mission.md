# Project Mission

This repo has one purpose: comparing performance of **optimized TensorRT Rust C++ FFI bindings** for STT, LLM, and TTS models against **basic ONNX Runtime** (CPU, CUDA, and TensorRT execution providers) versions of the same models.

**Target hardware:** Jetson Orin NX 16GB
